# Railway Deployment Guide

## 🚨 Помилка при першому деплою

Якщо бот не запускається з помилкою про `DATABASE_URL` або `RAILWAY_URL`, це нормально - потрібно налаштувати змінні.

## ✅ Кроки налаштування на Railway

### 1. Додайте PostgreSQL сервіс

- Перейдіть у вашому Railway проєкті
- Натисніть **"Add Service"** → **"Database"** → **"PostgreSQL"**
- Railway автоматично створить БД та змінну `DATABASE_URL`

### 2. Підключіть PostgreSQL до Bot сервісу

- Відкрийте **Bot** сервіс
- Перейдіть у вкладку **"Variables"**
- Натисніть **"Add Reference"** (або подивіться вже існуючу `DATABASE_URL`)
- Якщо `DATABASE_URL` не видна, обновіть сторінку
- Railway автоматично добавить `DATABASE_URL` з PostgreSQL сервісу

### 3. Додайте потрібні змінні в Bot сервісі

Перейдіть у **Bot Variables** та добавіть:

```env
TELEGRAM_BOT_TOKEN=8390037113:AAGObVg46v9dUJetALJXT2GyDwsKL4LtFWg
SUPER_ADMIN_ID=7364497786, 386680721
ENVIRONMENT=production
USE_WEBHOOK=true
RAILWAY_URL=https://yourdomain.railway.app
```

**ВАЖНО:**
- `RAILWAY_URL` повинна бути вашим доменом Railway (наприклад: https://tg-task-tracker.railway.app)
- Можна знайти у **Bot** → **"Settings"** → **"Domain"**
- Якщо домена немає, Railway створить її автоматично

### 4. Перероби (Redeploy)

- Натисніть **"Redeploy"** або зробіть новий commit
- Бот повинен стартувати без помилок

## 🔧 Як знайти RAILWAY_URL

1. Перейдіть на Bot сервіс
2. Натисніть **"Settings"**
3. Подивіться **"Domain"** - там буде ваш URL (наприклад: `https://tg-task-tracker-production.railway.app`)
4. Копіюйте це значення в змінну `RAILWAY_URL`

## 📋 Оточення

На Railway автоматично встановлюється:
- `RAILWAY_ENVIRONMENT=production` - Railway визначає, що ви на production
- `PORT=8080` (або інший) - Railway передає PORT

Наш код автоматично детектує Railway через ці змінні.

## ❌ Якщо все ще помилки

Перевірте **Bot Logs**:
1. Railway Dashboard → Bot сервіс
2. Натисніть **"View Logs"**
3. Подивіться на останні помилки

Найчастіші проблеми:
- `DATABASE_URL not set` → PostgreSQL не підключена, додайте її
- `RAILWAY_URL is required` → Додайте `RAILWAY_URL` в Variables
- `TELEGRAM_BOT_TOKEN` - Перевірте токен

## ✅ Успішний запуск

Якщо бачите в логах:
```
✓ PostgreSQL connection validated
✓ Bot configuration: ...
✓ Application started
```

То все працює! 🎉
